name: Test Kubernetes Runner

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-on-k8s:
    name: Run on K8s and Check Version
    runs-on: self-hosted
    steps:
      - name: 🚀 Test Runner
        run: |
          echo "🎉 Job is running on a self-hosted runner in Kubernetes!"
          echo "Runner Name: $RUNNER_NAME"

      - name: 🔍 Get Runner Version (Correct Path)
        run: |
          echo "Getting the runner version from the correct directory..."
          cd /runner
          ./run.sh --version
          echo "check the version of latest runner from github" 
